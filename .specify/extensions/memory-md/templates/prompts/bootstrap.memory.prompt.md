Set up the repository so memory is workflow-integrated, lightweight, and AI-readable.

Create or preserve:
- durable project memory files in `{memory_root}` (default `docs/memory`)
- compact memory index in `{memory_root}/INDEX.md`
- feature-local `{feature_memory_filename}` (default `memory.md`)
- feature-local `{memory_synthesis_filename}` (default `memory-synthesis.md`)
- Copilot instructions that require memory before planning, implementation, and verification

Summarize the four memory layers and what must never be stored in each.
Do not overwrite existing project-specific memory.
