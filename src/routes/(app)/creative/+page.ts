import type { PageLoad } from './$types';

export const load: PageLoad = () => {
  return {
    title: 'Creative Space',
    description: 'Brainstorm ideas, explore writing prompts, and capture creative inspiration.',
  };
};
