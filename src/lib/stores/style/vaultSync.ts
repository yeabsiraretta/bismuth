import { log } from '@/utils/logger';

/** Load CSS settings overrides from vault storage. */
export async function loadFromVault(): Promise<void> {
  log.debug('vaultSync: loadFromVault (stub)');
}

/** Initialize vault sync watcher for CSS settings changes. */
export function initVaultSync(): void {
  log.debug('vaultSync: initVaultSync (stub)');
}