/**
 * @deprecated — import from '@/services/startup/startupMetrics' instead.
 * This file re-exports for backward compatibility.
 */
export {
  startupHistory,
  lastDuration,
  recordStartupDuration,
} from '@/services/startup/startupMetrics';
export type { StartupEntry } from '@/services/startup/startupMetrics';
