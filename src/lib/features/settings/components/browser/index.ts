/**
 * Settings browser components live in src/lib/components/settings/
 * (e.g. ThemeBrowser.svelte) per the architecture convention that UI browser
 * components belong in the components/ layer. This stub reserves the
 * subdirectory for any future feature-scoped browser sub-components.
 *
 * T-10: no migration needed — components/settings/ is canonical.
 */
