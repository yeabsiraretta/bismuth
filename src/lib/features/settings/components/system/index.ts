/**
 * Settings system components live in src/lib/components/settings/system/
 * per the architecture convention that UI system components belong in the
 * components/ layer. This stub reserves the subdirectory for any future
 * feature-scoped system sub-components.
 *
 * T-10: no migration needed — components/settings/system/ is canonical.
 */
