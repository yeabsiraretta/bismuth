/**
 * Centralized Preset Data & Constants
 * Barrel re-export from sub-modules.
 */

export { IPC_PLACEHOLDERS } from './ipc-placeholders';
export { COLOR_PRESETS, ICON_PRESETS, KEYBOARD_SHORTCUTS, UI_CONSTANTS, VALIDATION_RULES, ERROR_MESSAGES, SUCCESS_MESSAGES } from './canvas-presets';
export { TEMPLATES, DEFAULT_SETTINGS } from './template-presets';

import { IPC_PLACEHOLDERS } from './ipc-placeholders';
import { COLOR_PRESETS, ICON_PRESETS, KEYBOARD_SHORTCUTS, UI_CONSTANTS, VALIDATION_RULES, ERROR_MESSAGES, SUCCESS_MESSAGES } from './canvas-presets';
import { TEMPLATES, DEFAULT_SETTINGS } from './template-presets';

export default {
  IPC_PLACEHOLDERS,
  COLOR_PRESETS,
  ICON_PRESETS,
  KEYBOARD_SHORTCUTS,
  DEFAULT_SETTINGS,
  TEMPLATES,
  VALIDATION_RULES,
  UI_CONSTANTS,
  ERROR_MESSAGES,
  SUCCESS_MESSAGES,
};
